# 数据库设计

> 当前 Schema 的完整快照，实现以本文为准。
> 决策过程见 ADR-0016、ADR-0017、`docs/学习区设计.md`、`docs/学习区设计-数据库影响.md`。

## 数据库的职责

**只做两件事：给前端提供渲染所需的读取性能，以及卡片管理。**

| 类型 | 去处 | 例子 |
| --- | --- | --- |
| 前端要渲染或编辑的 | 数据库 | 工作区列表、对话列表、卡片、提示词模板 |
| 卡片与调度 | 数据库 | card、card_schedule、review_log |
| 模型读写的散文 | 文件系统 | MISSION.md、GLOSSARY.md、USER.md、learning-records/ |
| 用户直接看的成品 | 文件系统 | essence/、assets/ |

提示词（AGENTS.md、style.md）存库，因为要在 WebUI 里切换和编辑。文件是数据库的投影。

## 存储边界

| 存储 | 内容 | 格式 |
| --- | --- | --- |
| Pi 会话文件 | 对话历史、工具调用、压缩产物 | JSONL，平铺在工作区目录下 |
| 业务数据库 | 本文所有表 | 单个 SQLite 文件 |

分库不合库：迁移历史各自演进（Pi 的会话 schema 尚未稳定，曾有不带迁移的破坏性变更）、写锁互不干扰、会话可单独归档而不动业务数据。两者之间没有需要原子保护的不变量。

选 SQLite，单用户部署（见 ADR-0015）。

## 两层会话模型

刻意保留 `session` 与 `pi_session` 的命名区分：

- **session** — 学习工作区，长生命周期，一个目录，含 N 次对话
- **pi_session** — 一次 Pi 对话，一个 jsonl 文件

`learning-records/` 存在的理由就是上下文会丢，说明工作区天然跨越多次对话。

**提示词挂在 pi_session 上，不挂在工作区**：同一个工作区里，这次可以用「新学」提示词，下次用「新学不制卡」，再下次用别的教学风格。提示词是开一次对话时的选择。

## 表关系

```
space ──< pi_session >── session
              │
              ├──> agents_md      提示词模板
              ├──> teach_style    教学风格
              └──> topic          review_topic_id，仅复习对话有值

topic ──< card ──1:1── card_schedule    仅 status='normal' 的卡有调度行
                 └──< review_log       软删除不级联，记录保留供 FSRS 优化
```

共 9 张表：

| 表 | 作用 |
| --- | --- |
| `space` | 会话类型枚举，3 行固定数据 |
| `session` | 学习工作区 |
| `pi_session` | 一次 Pi 对话，挂提示词与风格 |
| `agents_md` | AGENTS.md 提示词模板库 |
| `teach_style` | 教学风格库 |
| `topic` | 主题，卡片归类维度 + FSRS 参数配置粒度 |
| `card` | 卡片内容，含 `status` 三态（候选 / 正常 / 软删除） |
| `card_schedule` | FSRS 调度状态，与 card 一对一 |
| `review_log` | 复习记录，用于优化 FSRS 参数 |

## space

```sql
CREATE TABLE space (
  id       INTEGER PRIMARY KEY,   -- 0=助教(ta)  1=学习(learn)  2=复习(review)
  name     TEXT NOT NULL,
  version  INTEGER NOT NULL DEFAULT 0   -- 预留
);
```

初始化写入三行，之后不可增删。用整数主键因为要参与 JOIN。

| id | name |
| --- | --- |
| 0 | 助教 |
| 1 | 学习 |
| 2 | 复习 |

## session

学习工作区。一行对应一个目录。

```sql
CREATE TABLE session (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  name       TEXT NOT NULL,
  work_path  TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

只剩 3 个业务字段——工作区的状态几乎全在文件里，提示词和风格挂在 pi_session 上。

| 字段 | 说明 |
| --- | --- |
| `id` | 0 号是全局唯一的助教工作区（`~/pi-teacher/ta`），`name` 填「助教」。AUTOINCREMENT 从 1 开始，初始化时显式插入 id=0。 |
| `name` | **用户创建时手写的自由文本**。不由 AI 生成，AI 无权修改，后期可在 WebUI 改。 |
| `work_path` | 工作目录绝对路径，如 `~/pi-teacher/learn/5`。能由 id 推导，但根目录在 Docker 与本地部署下不同，显式存更稳。 |

用户对工作区的特殊要求写进 `learn/5/USER.md`，不进库。

## agents_md

AGENTS.md 提示词模板库。用户在 WebUI 里切换，切换即覆盖文件。

```sql
CREATE TABLE agents_md (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,
  description TEXT,          -- 给用户看的简介
  prompt      TEXT NOT NULL  -- AGENTS.md 的正文
);
```

预置模板（内容待提供）：

| 用途 | 说明 |
| --- | --- |
| 新学 | 默认学习模式，含制卡引导 |
| 新学不制卡 | 纯讲解，不提议卡片 |
| 复习 | 出题、判定、给反馈 |
| 助教 | 只答疑，不推进课程 |

单独建表而不是把正文存在 pi_session 里：模板要跨对话复用，存正文会导致每次开对话都复制一份全文，改模板还得批量更新历史行。

## teach_style

教学风格库，与 agents_md 正交——前者定「做什么」，后者定「怎么说话」。

```sql
CREATE TABLE teach_style (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,
  description TEXT,
  prompt      TEXT NOT NULL
);
```

预置若干行（循序渐进的老师、苏格拉底式追问等），用户可自定义。用表而不用 Skill 文件，因为用户要在界面上增删改（见 ADR-0006 修订）。

## pi_session

一次 Pi 对话。一行对应一个 jsonl 文件。

```sql
CREATE TABLE pi_session (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id       INTEGER REFERENCES session(id),
  space_id         INTEGER NOT NULL REFERENCES space(id),
  name             TEXT,
  path             TEXT NOT NULL UNIQUE,
  agents_md_id     INTEGER NOT NULL REFERENCES agents_md(id),
  teach_style_id   INTEGER REFERENCES teach_style(id),
  enable_make_card INTEGER NOT NULL DEFAULT 1,
  review_topic_id  INTEGER REFERENCES topic(id),
  created_at       TEXT NOT NULL DEFAULT (datetime('now')),

  CHECK (space_id = 2 OR review_topic_id IS NULL),
  CHECK (space_id = 1 OR session_id IS NULL OR session_id = 0)
);
```

| 字段 | 说明 |
| --- | --- |
| `session_id` | 归属工作区。学习对话指向具体工作区，助教恒为 0，复习为 `null`（跑在 `review/`）。 |
| `space_id` | 会话类型。 |
| `name` | 这次对话在干什么。**AI 唯一有权命名的东西**，首轮结束后由后端独立发起一次 LLM 调用生成，不在会话内走工具（避免污染学习上下文）。用户可改。Pi 官方无自动标题能力，这是自建的。 |
| `path` | jsonl 绝对路径，格式 `<ISO时间戳>_<sessionId>.jsonl`。`SessionManager.create()` 返回后立即 `getSessionFile()` 取得，**不需要回填**：路径在构造函数里就定了，延迟的只是写盘。 |
| `agents_md_id` | 本次对话用哪套提示词。开对话时选，投影成 `AGENTS.md`。 |
| `teach_style_id` | 本次对话用哪种风格。可空，空表示不注入风格。投影成 `style.md`。 |
| `enable_make_card` | 制卡开关。与 `space_id` 组合覆盖「新学 / 新学不制卡 / 复习 / 复习不制卡」四种意图。助教对话不看此字段，工具层直接拒绝制卡。 |
| `review_topic_id` | 复习对话的主题过滤，`null` = 不限制。CHECK 保证其他类型恒为 null。可随时改，只影响之后的查询。 |

一个工作区允许多个并发 pi_session。`learning-records/` 编号与 `GLOSSARY.md` 的并发写冲突**不处理**：概率低，后果轻。

**子代理会话不建记录。** 资料抓取子代理（见 ADR-0016）的 jsonl 也会落在工作区目录里，但 `pi_session` 表只记录用户直接参与的对话。理由回到数据库职责：子会话既不需要前端渲染，也不涉及卡片管理，建行只会让每个列表查询都得带一个排除条件。

代价是要靠文件系统识别它们：子会话的 jsonl header 里有 `parentSession` 字段指向父会话文件。排查抓取失败时，扫描工作区目录、按这个字段匹配即可找到对应子会话。清理旧子会话同理——按 header 判断，不查库。

## topic

```sql
CREATE TABLE topic (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  name              TEXT NOT NULL UNIQUE,
  description       TEXT,
  request_retention REAL NOT NULL DEFAULT 0.9,
  maximum_interval  INTEGER NOT NULL DEFAULT 365,
  created_at        TEXT NOT NULL DEFAULT (datetime('now'))
);
```

| 字段 | 说明 |
| --- | --- |
| `name` | 全局唯一。可细化到任意粒度，如「后端」「Java」「JVM」。制卡时 AI 读现有列表优先复用，避免同义词泛滥。 |
| `description` | 给 AI 做归类判据。「Java」可写「Java 语言核心、JVM、并发、集合框架、IO 等」。 |
| `request_retention` / `maximum_interval` | FSRS 参数，主题级配置。核心算法可要 95% 保留率，边缘知识 80% 即可。 |

## card

```sql
CREATE TABLE card (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  topic_id            INTEGER NOT NULL REFERENCES topic(id),
  front               TEXT NOT NULL,
  back                TEXT NOT NULL,
  status              TEXT NOT NULL DEFAULT 'proposed',
  reason_and_remark   TEXT,
  source_essence_path TEXT,
  created_at          TEXT NOT NULL DEFAULT (datetime('now')),

  CHECK (status IN ('proposed', 'normal', 'deleted'))
);
```

| 字段 | 说明 |
| --- | --- |
| `front` / `back` | 正面（问题）与背面（参考答案或评价标准）。 |
| `status` | 卡片状态，三档，见下。 |
| `reason_and_remark` | AI 写的补充说明与制卡原因。取代了原来的溯源字段——「为什么造这张卡」用一段话讲清楚，比存一个消息 id 更有用，也不用去解析 jsonl。 |
| `source_essence_path` | 卡片出自哪个 essence 文件，**绝对路径**，可为 null。复习时能跳回精华文档看全貌。存路径不存外键，因为 essence 不建表；文件被删则字段悬空，复习界面按「文件不存在就不显示跳转」处理。 |

### status 三档

| 值 | 含义 | 谁写入 |
| --- | --- | --- |
| `proposed` | 待用户审批的候选卡 | 制卡工具固定写这个值 |
| `normal` | 已确认，参与复习 | 用户在 WebUI 确认时，或用户手动建卡时 |
| `deleted` | 软删除 | 删卡工具、用户删除操作 |

**AI 没有真正的建卡与删卡权限，只能改状态。** 制卡工具固定写 `proposed`，删卡工具固定写 `deleted`，两者都不做物理写入或物理删除。这让暴露给模型的工具面变得很小，也让误操作可恢复。

**读取只认 `normal`。** 到期队列、卡片总数、复习统计一律带 `status = 'normal'` 条件。候选卡只出现在待审批列表，软删除的卡只出现在回收站视图。

`DEFAULT 'proposed'` 是有意的保守取值：遗漏赋值时最坏结果是多一张待审批的卡，而不是未经确认的卡直接混进复习队列。用户手动建卡时显式传 `normal`——手动创建本身即用户意图，不需要再确认（见 ADR-0009）。

### 与 card_schedule 的关系

`card_schedule` 行在卡片转为 `normal` 时才创建，`proposed` 状态的卡没有调度状态——它还不参与复习，凭空给一个 FSRS 初始状态没有意义，还会让「到期卡片数」这类统计需要额外排除条件。

软删除**不删** `card_schedule` 行。这样从回收站恢复时，复习进度原样回来。

### 软删除对 FSRS 的额外好处

`review_log` 通过 `ON DELETE CASCADE` 引用 `card`。软删除不触发级联，所以已删除卡片的复习记录仍然保留。这些记录对 FSRS 参数优化依然是有效样本——用户的记忆规律不因为删了某张卡而改变。

物理删除只发生在用户明确清空回收站时，那时才会级联带走 `review_log`。

去掉了三个字段：

**`answer_mode`** — 评价方式交给 AI 判断，这才是灵活性所在。硬编码三档（open_ended / exact / code）反而限制了模型：同一张卡在用户答得完整时该夸，答得含糊时该追问，这些都不是枚举能表达的。需要类型特定的信息（rubric、复杂度要求）就写进 `back` 或 `reason_and_remark`，模型自己看得懂。`metadata` 一并去掉，同理。

**`source_message_id`** — 长对话后从 jsonl 里定位某条消息性能很差（要全文扫描），而拿到原文的实际收益很低。

**`source_pi_session_id`** — 同上，`reason_and_remark` 已经承载了它的意义。

## card_schedule

```sql
CREATE TABLE card_schedule (
  card_id        INTEGER PRIMARY KEY REFERENCES card(id) ON DELETE CASCADE,
  state          TEXT NOT NULL DEFAULT 'new',
  due            TEXT NOT NULL,
  stability      REAL NOT NULL,
  difficulty     REAL NOT NULL,
  elapsed_days   INTEGER NOT NULL DEFAULT 0,
  scheduled_days INTEGER NOT NULL DEFAULT 0,
  reps           INTEGER NOT NULL DEFAULT 0,
  lapses         INTEGER NOT NULL DEFAULT 0,
  last_review    TEXT,

  CHECK (state IN ('new', 'learning', 'review', 'relearning'))
);
```

FSRS 状态与卡片内容分离：调度状态每次复习都变，卡片内容基本不变，混一张表会让每次复习重写整行。

| 字段 | 说明 |
| --- | --- |
| `state` | FSRS 四状态。 |
| `due` | 下次到期时间，ISO 8601。到期队列的查询条件。 |
| `stability` / `difficulty` | FSRS 核心参数。 |
| `elapsed_days` / `scheduled_days` / `reps` / `lapses` | FSRS 统计字段。 |
| `last_review` | 上次复习时间。 |

字段命名与 `ts-fsrs` 的 `Card` 对象一致，避免映射层。

## review_log

**保留，用途是优化 FSRS 参数，不是给用户看复习历史。**

```sql
CREATE TABLE review_log (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  card_id        INTEGER NOT NULL REFERENCES card(id) ON DELETE CASCADE,
  rating         TEXT NOT NULL,
  state          TEXT NOT NULL,
  due            TEXT NOT NULL,
  stability      REAL NOT NULL,
  difficulty     REAL NOT NULL,
  elapsed_days   INTEGER NOT NULL,
  scheduled_days INTEGER NOT NULL,
  reviewed_at    TEXT NOT NULL DEFAULT (datetime('now')),

  CHECK (rating IN ('Again', 'Hard', 'Good', 'Easy'))
);
```

字段是 `ts-fsrs` 的 `ReviewLog` 结构——**FSRS 参数优化器（`fsrs-optimizer`）需要的就是这个格式的复习序列**，字段对齐可以直接喂给它，不做转换。

砍掉了三类字段：

- **`pi_session_id`** — 那是审计用的（「这次 Again 发生在哪次对话」），优化算法不需要。
- **`ai_evaluation`** — AI 给用户的反馈是一次性的，看完就过去了。FSRS 只吃 `rating`，评语对算法无用。要留痕就让 AI 写进 `card.reason_and_remark`。
- **before/after 双份快照** — 优化器只需要「复习时的状态 + 用户的 rating」。`after` 状态可以由 FSRS 从 before + rating 重算出来，存两份是冗余。

`rating` 由 AI 判定四档。这是 AI 唯一要输出的结构化结果，其余评价都是给用户看的自然语言，不入库。

## 目录布局

```
~/pi-teacher/
  ├─ AGENTS.md              ← 全局规则：工具调用原则、制卡规则、资料库说明
  ├─ USER.md                ← 全局用户画像与偏好
  │
  ├─ materials/             ← 全局资料库，三种会话共用，全部以 AI 可读格式存储
  │   ├─ index.md           ← 索引，普通 Markdown，模型自己维护
  │   ├─ origins/           ← 原件（PDF 等 AI 不可直读的格式）
  │   ├─ about-docker/      ← 含图片的 HTML 以文件夹形式存储
  │   │   ├─ index.html
  │   │   └─ imgs/
  │   └─ 李宏毅机器学习/      ← 系列课程按课程名建文件夹
  │       ├─ lesson1-梯度下降.md
  │       └─ lesson2-反向传播.md
  │
  ├─ assets/                ← 跨工作区复用的样式表、测验小部件、图表工具、HTML 模板
  │
  ├─ learn/
  │   ├─ 5/                 ← 目录名 = session.id
  │   │   ├─ AGENTS.md      ← 程序生成，内容 = agents_md.prompt
  │   │   ├─ style.md       ← 程序生成，内容 = teach_style.prompt
  │   │   ├─ USER.md        ← 用户手写，本工作区的特殊要求
  │   │   ├─ MISSION.md     ← 为什么学这个、掌握程度需求
  │   │   ├─ GLOSSARY.md    ← 本工作区的标准术语
  │   │   ├─ essence/       ← 用户会反复查阅的精华文档，制卡资料来源
  │   │   ├─ learning-records/  ← 0001-slug.md 顺序编号，只给 AI 看
  │   │   ├─ assets/        ← 本工作区专用资源，不复用
  │   │   └─ *.jsonl        ← Pi 会话文件
  │   └─ 12/
  │
  ├─ review/                ← 平铺，无编号子目录
  │   ├─ AGENTS.md
  │   ├─ style.md
  │   └─ *.jsonl
  │
  └─ ta/                    ← 助教，session.id = 0
      ├─ AGENTS.md
      └─ *.jsonl
```

**Pi 会话文件存在我们指定的目录**：`SessionManager.create(workPath, workPath)` 把 `sessionDir` 指向工作目录本身，jsonl 平铺在那里，不需要 `.pi/sessions/` 子目录。

**AGENTS.md 自动拼接**（Pi 从 cwd 向上遍历到根）：

1. `~/pi-teacher/AGENTS.md` — 全局通用规则
2. `~/pi-teacher/learn/5/AGENTS.md` — 本次对话选定的提示词

`materials/index.md` 不由程序渲染进 AGENTS.md，全局 AGENTS.md 里写一段引导让模型自己读。

## 文件与数据库的同步

`AGENTS.md` 和 `style.md` 是数据库的投影，**新开 pi_session 时按选定的模板整份覆盖**：

```
新开对话（用户选了 agents_md 和 teach_style）
  → 读 agents_md.prompt   → writeFile('learn/5/AGENTS.md', ...)
  → 读 teach_style.prompt → writeFile('learn/5/style.md', ...)
  → SessionManager.create(workPath, workPath)
```

同一个工作区，这次用「新学」下次用「复习」，切换即覆盖。WebUI 里改模板内容走同一条路：改 `agents_md.prompt`，下次开对话生效。

**用户手动改这两个文件我们不管**——下次开对话会被覆盖，不做冲突检测，不做回读合并。数据库单向是源。

分成两个文件而不是拼成一个：`agents_md` 定「做什么」、`teach_style` 定「怎么说话」，两者正交、可自由组合，拼在一起就得为每种组合存一份全文。AGENTS.md 里写一段引导指向 `style.md` 和 `USER.md`。

## 助教的「知道当前学习上下文」

前端传 `session_id`，后端读该工作区的 `AGENTS.md`，连同 `work_path` 一起拼成 system-reminder 注入助教这一轮请求。不读 MISSION.md、GLOSSARY.md、learning-records——给了路径，助教需要时自己用文件工具读。助教仍在 `ta/` 目录运行。

## 待定项

- `agents_md` 四套预置模板的正文（用户提供）
- `teach_style` 预置风格的正文
- 索引尚未设计，见 `todo.md`。已知最关键的一条是到期队列查询会同时用到 `card.status` 和 `card_schedule.due`，联合方式实现时定
- 助教「知道当前学习上下文」的注入走 `context` 事件（见 `docs/pi-hook机制调研.md`），具体注入哪些内容待定

已决的相关事项：

- 难题清单（sticking points）**不建表，术语废弃**。卡片 + `reason_and_remark` 已完全替代它，学习过程中的卡点写进 `learning-records/`。见 ADR-0009。
- FSRS 参数优化**完全手动触发**，后端提供接口给前端调用。见 `todo.md`。
- 每轮动态内容注入走 Pi 的 `context` 事件，不写入 jsonl、不受压缩影响。静态提示词仍走文件投影。见 `docs/pi-hook机制调研.md`。
