系统整体分层，需完美兼容docker：
操作系统--
nodejs运行时  数据库
agent底层 pi {
    {mcp
    skill} 用于扩展agent能力
    插件 对pi本身的流程做修改，例如修改系统提示词 启停其他工具  访问当前 session 监听 before_agent_start 拦截或阻止其他工具调用 修改工具参数或工具结果
} 「
    沙箱-通过插件实现： 这个插件封装我们自研的用docker实现的暴露ssh端口、有语言运行时的容器实现，专门用来执行代码，并将市面上一些成熟的商业产品做封装为统一的api，可以自主切换。同时修改pi自带的 bash 工具的表述为 ： 用于本机调用，在执行代码验证时，应当使用沙箱，本机禁止安装语言运行时(除了nodejs 这个必须) 

」
对pi的封装 对数据库操作的封装

webui





外部：

