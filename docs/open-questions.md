# 未决问题

已确认的决策见 [`adr/`](./adr/)，Schema 见 [`../数据库设计.md`](../数据库设计.md)，领域语言见 [`../CONTEXT.md`](../CONTEXT.md)。本文只记录尚未拍板的事项。

原先阻塞工具定义的三条（卡片去重判据、复习形态、Rating 判定规则）均已拍板，见下方「已在别处解决的」。工具定义可以开始了。

## 卡片与复习

1. **一批取多少张卡。** 到期的卡可能远多于一场对话该问的量。按固定张数取（如 20 张）、按 Topic 取完整一组、还是让用户在开始复习时自己选？
ans:定在设计取卡工具时,目前的打算时，让ai自己选，全局提示词中我们不是要写工具调用原则嘛，让他结合用户偏好和对用户的能力判断程度，自主选择，我们提的取卡工具提供一个 nums参数就行了。

2. **到期卡积压怎么办。** 停用一周后回来，到期卡堆了两百张。是一次性全塞进队列（用户会放弃），还是每天限量、其余顺延（顺延会让 FSRS 的间隔计算失真）？ 
ans: 这个问题其实同1 ，这种情况其实经常发生，保持在队列里，不做调整，让ai自己选 nums参数，以及，我们的卡片不仅要支持ai去读，也要提供一个webui，提供不使用ai辅助，自己直接去做复习，并选择记忆程度的功能，

3. **每张卡要问多久，能不能预估。** 若能，取卡时就可以凑出「大约 20 分钟一批」而不是「固定 20 张」——一张概念卡半分钟，一张算法卡五分钟，按张数取会让每批耗时忽高忽低。耗时不该让用户手填，可以从 `review_log` 的时间间隔统计出来。这依赖第 2 项，且需要真实数据，首版可以先不做。
 ans: 完全不做这个限制，交给ai去判断，这些东西完全可以由用户和ai对话时，写入到全局级的user.md 作为用户偏好或者会话级的 agents.md 这些由用户和ai的对话中他们来决定，我们不做约束。
4. FSRS 个性化参数训练最少需要多少条有效复习记录？触发方式已定为完全手动（见 `../todo.md`），但参数写回 `topic` 表还缺 `w` 数组字段。
 ans:我记得fsrs训练本身就对条数有约束，条数不足时，传进去最后给出的算法权重不会改变，也就是说，我们本身不做限制，即使0条，也允许使用。

## 上下文与工作区

5. 上下文组装器每轮通过 `context` 事件注入哪些内容？已确认走这条通道（见 `pi-hook机制调研.md`），但注入项清单与预算分配未定——待复习卡数、当前使命摘要、最近学习记录各占多少。已掌握术语明确**不注入**，由模型自己查（见 ADR-0020）。
6. 历次对话的摘要如何存储、如何分层（最近几次详细 + 更早合并）、何时触发合并？还是首版不做，只靠 Pi 的自动压缩？
ans: 只使用pi的自动压缩，learn-record，user.md 和MISSION.md  以及术语表的设计，足够ai推断当前的学习情况和用户能力了 
7. **`review/` 目录的 AGENTS.md 从哪来。** 换个说法：`learn/5/AGENTS.md` 和 `ta/AGENTS.md` 都是程序从 `agents_md` 表写出来的（前者按用户开对话时的选择，后者固定用 `id=0` 那行）。`review/AGENTS.md` 呢？开复习对话时也从表里的「复习」模板写出来，还是就在 `review/` 目录里放一个手写的文件、程序不管它？前者一致但要占一行模板，后者省事但破坏了「提示词都在库里、WebUI 可编辑」的规则。

## 提醒与周期任务

8. 卡片提醒的默认轮次是多少？用户偏好提醒 30 轮是否合适。
9. 提醒合并的具体逻辑（卡片提醒 + 偏好提醒同时触发时如何组装成一条）。

## 视觉与内容生成

10. AI 生图服务与模型（Stable Diffusion / DALL-E / 其他 API）、图片存储方式、预算限制和重试策略。
11. 生图触发方式：用户明确要求、AI 主动提议（如背单词自动配图）、还是两者并存。

## 前后端

12. 后端为不同工作区维护独立的 Pi 实例，还是动态切换工作目录？前者更干净但资源占用更高。
13. Web 后端框架（Express / Fastify / Hono / tRPC）。若抄 pi-web 的桥接层，跟着用 Next.js API Routes 最省事（见 `pi-web-研究/01-桥接层.md`）。
14. 前端框架（React / Vue / Svelte）与状态管理方案。同上，抄 pi-web 则跟 React。
15. 卡片管理与复习界面的具体交互与布局。复习界面是对话窗口（见 ADR-0018），可参考 pi-web（见 `pi-web-研究/03b`、`03e`）。
16. Docker 镜像基础选择（Node Alpine / Debian Slim），SQLite 是否需要单独挂载卷。
17. 用户认证与多用户隔离：首版纯本地单用户，何时引入认证？多用户时按用户目录还是数据库 tenant_id 隔离？
18. 备份与迁移：用户如何导出全部数据（jsonl + SQLite + 资料文件）？

## 其他

19. 手机端复习若成为需求，走单向导出 apkg（见 ADR-0008）。是否要做、什么时候做。

## 已在别处解决的

以下问题曾列在本文，现已有答案，记录出处避免重复讨论。

| 问题 | 结论 | 出处 |
| --- | --- | --- |
| 工作目录组织方式 | `~/pi-teacher/learn/<session_id>/` | `数据库设计.md` |
| 资料存哪、索引格式 | 全局 `materials/`，索引是 `index.md` 普通 Markdown，模型自读自写，不做程序解析 | `数据库设计.md` |
| 对话标题生成 | 后端独立 LLM 调用，prompt 为「总结下面的内容，生成一个 20 字以内的标题」 | `数据库设计.md` |
| 助教会话压缩参数 | 保持 Pi 默认 | — |
| AGENTS.md 生成时机 | 新开对话时从 `agents_md` 表整份覆盖写出 | `数据库设计.md` |
| 卡片表字段 | 6 个字段 + `status` 三态，无 `answer_mode` 与 `metadata` | `数据库设计.md` |
| 卡片溯源 | 不存消息 id 与对话外键，靠 `reason_and_remark` | ADR-0010 |
| 难题清单数据结构 | 不建表，Sticking Point 术语废弃 | ADR-0009 |
| Pi 会话存储格式 | JSONL + 自定义 `sessionDir` 指向工作目录 | `数据库设计.md` |
| Pi 自动标题 | 官方无此能力，自建 | — |
| 每轮动态注入机制 | Pi 的 `context` 事件，不落盘、不受压缩 | `pi-hook机制调研.md` |
| Pi 事件流转 SSE | pi-web 已有实现，可原样抄三个文件 | `pi-web-研究/01-桥接层.md` |
| Mermaid 渲染与失败回退 | pi-web 的 `MermaidBlock.tsx` 有现成实现 | `pi-web-研究/03a-Markdown管线.md` |
| 资料抓取的上下文污染 | 交给子代理，`inheritContext: false` | ADR-0016 |
| HTML 输出服务端渲染 | 首版不做 | — |
| 是否集成 Anki | 不集成 | ADR-0008 |
| 卡片去重判据 | 精确匹配（同 Topic、`front` 归一化）归代码，语义相似归模型；首版不用向量 | ADR-0017 |
| 复习形态 | 取一批 → 组织对话 → 批量判定 | ADR-0018 |
| Rating 判定规则 | 模型直接输出四档，无中间映射层；每档含义写在 rubric 里，待细化 | ADR-0019 |
| 卡片提议确认 UI | 批量确认与零星确认共用同一套交互 | — |
| 合并卡的判据与实现 | 纯提示词约束触发时机 + 合并工具做实际合并与 FSRS 状态更新，不让 AI 算；做进制卡 skill | `todo.md` |
| 学习计划 | 由 `MISSION.md` 承担，不做「每天学什么」的拆分 | `todo.md` |
| 视频转文本、网页抓取服务 | 后续由 skill 接第三方实现；视频下载用 yt-dlp，网页抓取用用户已有工具 | `todo.md` |
| 沙箱选型 | 设计工具时再定 | — |
| 已掌握术语怎么给模型 | 不注入，`USER.md` 里写引导，AI 自主决定何时用数据库工具查 | ADR-0020 |

## 已核实的外部事实

以下来自对源码的核实，供决策参考，本身不是结论。

### Pi（`earendil-works/pi`，核实于 v0.84.2）

- 默认会话存储是 JSONL，条目以 `id` / `parentId` 构成树结构，压缩不删除旧条目，完整历史可用于 Web UI 展示。
- 官方存在 SQLite 会话后端，但属 pre-stabilization：schema 近期发生过一次不带迁移的破坏性变更。
- 存在可自行实现的会话仓库与存储接口，是落到自有存储的正规入口。
- **没有官方的自动标题生成能力**，标题需自建。
- 会话头部没有自由 metadata 字段槽位，自定义业务数据需走自定义条目通道。
- **`context` 事件**：每次 LLM 调用前触发，返回值只进请求 payload，不写 jsonl、不受压缩影响，是每轮动态注入的正规通道。`before_agent_start` 注入的 `role: "custom"` 消息会落盘且参与压缩，不适合此用途。
- 自动压缩可配置触发阈值与保留窗口，压缩产物是追加的条目，原始消息保留。
- `AGENTS.md` 机制：全局 `~/.pi/agent/AGENTS.md`（是 `agent/` 不是根 `.pi/`）+ 工作目录 `AGENTS.md`（是 cwd 本身，不是 `.pi/AGENTS.md`）。不受压缩影响。**`@文件` 引用不存在于 AGENTS.md 内部**，它是 CLI 参数语法，不是文件 import。
- `SessionManager.create(cwd, sessionDir)` 可自定义会话存储目录，JSONL 平铺在 `sessionDir` 下。
- **延迟落盘但路径提前确定**：文件名在构造时生成（`<时间戳>_<sessionId>.jsonl`），`getSessionFile()` 创建后立即可取，写盘动作延迟到首条 assistant 回复。所以路径不需要回填。
- `create(cwd, sessionDir, options)` 的 `options.id` 可自定义 sessionId（须匹配 `/^[A-Za-z0-9](?:[A-Za-z0-9._-]*[A-Za-z0-9])?$/`），会进入文件名。
- `open(path, sessionDir, cwdOverride)` 可完全自定义文件路径，但 sessionId 随机不可指定，`cwd` 需显式传第三参否则退化为 `process.cwd()`。传入 0 字节的已存在文件会让 header 当场落盘。
- 原生无 subagent，但可用 `InlineExtension` + `defineTool` 自行实现（pi-web 的做法，MIT 可复用）。

### Anki（已决定不集成，见 ADR-0008）

- AnkiConnect 是社区插件，寄生在桌面进程内，要求 Anki 常驻；官方无 HTTP API，且明确拒绝此类贡献。
- `answerCards` 支持 ease 1–4 对应四档评级，走真实调度器，不需要 GUI 在复习界面。
- Anki 持有 collection 的独占锁，第三方进程无法并发读写。
- 卡片级 FSRS 记忆状态在 `cards.data` JSON 列中，AnkiConnect 未暴露。
- 官方 Python `anki` 包是能力最全的无 GUI 路径，但需要 Python sidecar。
- `ts-fsrs` 实现 FSRS-6，参数与 Anki 同源同序，日后需要互操作时可映射。
