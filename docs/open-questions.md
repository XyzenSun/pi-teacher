# 未决问题

已确认的决策见 [`adr/`](./adr/)，领域语言见 [`../CONTEXT.md`](../CONTEXT.md)。本文只记录尚未拍板的事项。

## Session 与工作目录

1. ~~学习会话的工作目录按什么维度组织？~~ **已决**：按首个 session id 建目录（`~/pi-teacher/learn/5/`），多个 session 可共享同一 `work_path`，前端按 `work_path` 分组展示。见 ADR-0016。
2. ~~资料存储在会话目录下还是主题目录下？~~ **已决**：存在会话组目录的 `materials/` 下，由 `index.json` 索引。见 ADR-0016。
3. 学习会话的 `name`（标题）生成时机：首轮对话结束后，后端独立发起 LLM 调用生成。具体用什么 prompt、失败时如何回退（生成默认标题 vs 留空 vs 重试）？
4. 资料索引 `index.json` 的具体格式：需要包含哪些字段（文件路径、摘要、来源会话、上传时间、文件类型）？由谁维护（上传资料的工具写、还是上下文组装器生成）？
5. 助教会话的压缩参数：`reserveTokens` 从默认 16384 降到多少（建议 4096 或 8192）？
6. 上下文组装器生成 `AGENTS.md` 的时机：每次会话启动前重新生成，还是只在业务数据变更（新增资料、新增难题）时更新？

## 卡片建模

7. ~~卡片表的最终字段~~ **已决**：单表 + `answer_mode` 判别（`open_ended` / `exact` / `code`）+ `metadata` JSON 字段存额外数据。见 ADR-0017。
8. ~~卡片的 `source_message_id` 格式~~ **已决**：存 Pi 会话内的 entry id，跳回原文时读 `pi_session_path` 解析 JSONL。见 ADR-0017。
9. 合并卡（多个低难度问题合成一张卡）的具体判据、AI 如何整体评级、何时触发分裂？
10. 卡片提议的批量确认 UI（导入笔记场景）与零星确认 UI（学习中制卡）是否共用同一套交互？
11. 卡片去重的判据：同 `work_path` 组内 `front` 完全相同就拒绝，还是允许语义相似但表述不同的卡片？

## Briefing 与上下文注入

12. ~~`.pi/AGENTS.md` 的生成方式~~ **已决**：由上下文组装器生成，不手写。具体生成时机见第 6 项。
13. 历次会话的摘要如何存储、如何分层（最近几场详细 + 更早的合并）、何时触发合并？还是首版不做会话摘要，只靠 Pi 的自动压缩？
14. 学习计划（你之前提到的"动态可调整"）要不要做、挂在哪、谁生成、谁触发更新？
15. 资料文件（B 站视频转文本、文档抓取）全文存储还是只存摘要 + 原文路径、按需加载的工具如何设计？视频转文本、抓取清洗用什么服务？
16. 难题清单的数据结构：是独立的表（`sticking_point` 表）还是 `session` 的关联数据？如何标记"已解决"？

## 复习机制

17. 评价 rubric 的具体内容，以及从模型评价落到四档 Rating 的判定规则。
18. 复习是逐卡问答、按主题分组对话，还是两种模式并存。
19. 到期队列的分组、每日上限、积压处理和特殊日期处理。队列还需按预估耗时做负载均衡——耗时不手填，由复习记录的历史统计得出。
20. 是否启用 FSRS 个性化参数训练，以及最少需要多少条有效复习记录。
21. 远程代码沙箱具体选哪家（Judge0、Piston、自建等），接入形态倾向 MCP。算法卡首版不强制写代码，沙箱是后续增强。

## 提醒与周期任务

22. Hook 的默认卡片提醒轮次（暂定多少？）；用户偏好提醒 30 轮是否合适。
23. 提醒合并的具体逻辑（卡片提醒 + 偏好提醒同时触发时如何组装成一条）。

## 视觉与内容生成

24. AI 生图服务、模型（Stable Diffusion / DALL-E / Midjourney API）、图片存储方式、预算限制和重试策略。
25. 生图触发方式：用户明确要求、AI 主动提议（如背单词时自动配图）、还是两者并存。
26. Mermaid 在 Web UI 中的渲染、安全过滤和失败回退。
27. ~~HTML 输出能力的服务端渲染方案与上线时机~~ **暂缓**：首版不做。

## Pi 集成与前后端

28. ~~Pi 会话存储采用默认 JSONL 还是官方 SQLite session backend~~ **已决**：用 JSONL + 自定义 `sessionDir`，存在会话组目录内。见 ADR-0016。
29. 后端为不同 `work_path` 维护独立的 Pi 实例，还是动态切换工作目录？前者更干净但资源占用更高。
30. Pi 的事件流如何转换为前端需要的 SSE / WebSocket 格式？流式输出的错误处理与重连机制。
31. ~~Pi 自动标题生成~~ **已决**：Pi 官方无此能力，自建独立 LLM 调用生成。见 ADR-0016。
32. Web 后端框架（Express / Fastify / Hono / tRPC）。
33. 前端框架（React / Vue / Svelte）与状态管理方案。
34. 前端的会话列表、历史消息、卡片管理、复习界面的具体交互与布局。
35. Docker 镜像的基础选择（Node Alpine / Debian Slim），以及 SQLite 是否需要单独挂载卷。
36. 用户认证与多用户隔离：首版是纯本地单用户，何时引入认证？多用户时数据隔离策略（按用户目录 vs 数据库 tenant_id）。
37. 备份与迁移：用户如何导出所有数据（会话 JSONL + 业务 SQLite + 资料文件）？

## 其他

38. 用户是否需要导出学习空间、资料、卡片和学习记录。手机端复习若成为需求，走单向导出 apkg（见 ADR-0008）。

## 已核实的外部事实

以下来自对源码与文档的核实，供上述决策参考，本身不是结论。

### Pi（`earendil-works/pi`）

- 默认会话存储是 JSONL，条目以 `id` / `parentId` 构成树结构，压缩不删除旧条目，完整历史可用于 Web UI 展示。
- 官方存在 SQLite 会话后端，但属 pre-stabilization：其 schema 近期发生过一次不带迁移的破坏性变更。
- 存在可自行实现的会话仓库与存储接口，是落到自有存储的正规入口。
- **没有官方的自动标题生成能力**，标题需自建。
- 会话头部没有自由 metadata 字段槽位，自定义业务数据需走自定义条目通道。
- 生命周期钩子完备，支持每轮请求前注入消息与修改 system prompt，足以支撑周期提醒。
- 自动压缩可配置触发阈值与保留窗口，压缩产物是追加的条目，原始消息保留。
- `AGENTS.md` 机制：全局 `~/.pi/agent/AGENTS.md`（注意是 `agent/` 不是根 `.pi/`）+ 工作目录 `AGENTS.md`（注意是 cwd 本身，不是 `.pi/AGENTS.md`）作为系统提示词，不受压缩影响。**`@文件` 引用不存在于 AGENTS.md 内部**，它是 CLI 参数语法，不是文件 import。
- SDK 支持通过 `SessionManager.create(cwd, sessionDir)` 自定义会话存储目录，JSONL 文件会平铺在 `sessionDir` 下，不再按 cwd 编码建子目录。
- 同进程多实例安全，无全局状态冲突。
- **延迟落盘**：首条 assistant 回复之前 JSONL 文件不存在，`pi_session_path` 需在首轮结束后回填。

### Anki（已决定不集成，见 ADR-0008）

- AnkiConnect 是社区插件，寄生在桌面进程内，要求 Anki 常驻；官方无 HTTP API，且明确拒绝此类贡献。
- `answerCards` 支持 ease 1–4 对应四档评级，走真实调度器，不需要 GUI 在复习界面。
- Anki 持有 collection 的独占锁，第三方进程无法并发读写。
- 卡片级 FSRS 记忆状态在 `cards.data` JSON 列中，AnkiConnect 未暴露。
- 官方 Python `anki` 包是能力最全的无 GUI 路径，但需要 Python sidecar。
- `ts-fsrs` 实现 FSRS-6，参数与 Anki 同源同序，日后需要互操作时可映射。
