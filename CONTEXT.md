# Pi Teacher

一个 Web 学习助手。它通过多种学习风格帮助用户理解知识、练习知识，并让值得长期保持的知识真正留下来。

本文只定义领域语言，不记录实现方案、技术选型或字段设计。Schema 见 `数据库设计.md`。

## Language

### 会话

**Session（学习工作区）**：
一个长期的学习目标及其全部产出。它对应一个目录，装着使命、术语表、学习记录、精华文档，以及若干次对话。名称由用户创建时手写，AI 无权修改。助教工作区全局唯一。
_Avoid_: 学习空间、StudySpace、Workspace、Session Group、会话组、项目

**Pi Session（对话）**：
一次完整的多轮对话，归属于某个 Session。开始时选定提示词模板与教学风格，之后不变。名称由 AI 在首轮结束后生成，是 AI 唯一有权命名的东西。
_Avoid_: 会话、conversation、chat、thread

**Space（空间）**：
对话的类型标记，三个固定值——学习、复习、助教。不是可创建的实体。
_Avoid_: 模式、学习模式、复习模式

**Turn（轮次）**：
对话中一次「用户发言 + 助手回应」的来回。周期性提醒以轮次计数。
_Avoid_: 消息、message、round

### 提示词

**Agents Md（提示词模板）**：
决定一次对话「做什么」的提示词——新学、新学不制卡、复习、助教等。用户可自定义，在开对话时选定。
_Avoid_: 系统提示词、system prompt、mode

**Teach Style（教学风格）**：
决定一次对话「怎么说话」的提示词——循序渐进的老师、苏格拉底式追问等。与提示词模板正交，可自由组合。用户可自定义。
_Avoid_: 人格、角色、persona

**Briefing（上下文注入）**：
向模型注入的当前状态。静态部分（提示词模板、教学风格）在开对话时投影为文件；动态部分（待复习卡片数、学习进度）在每轮请求前注入，不进对话历史，不受压缩影响。
_Avoid_: 系统提示词、上下文窗口、历史摘要

**Reminder（提醒）**：
按轮次注入给模型的隐式提示，让模型考虑是否需要维护卡片。接收者是模型，不是用户；用户界面不因提醒而弹窗。
_Avoid_: 通知、prompt、催促

### 工作区产出

以下全部是文件，模型读写，不进数据库。

**Mission（使命）**：
一个学习工作区中的文件，讲解为什么要学这个知识、要掌握到什么程度。它是整个学习体系的根基——没有使命，课程就会抽象化，无法决定下一步学什么。
_Avoid_: 学习目标、目标、goal

**Glossary（术语表）**：
一个学习工作区中的标准语言。所有讲解、练习和学习记录都遵循其中的术语。能把一个概念压缩成简明定义，正是用户已经理解它的证明。
_Avoid_: 词汇表、名词解释

**Learning Record（学习记录）**：
按顺序编号的学习进度记录，只给 AI 看，用户不会翻。它存在的理由是对话上下文会丢。
_Avoid_: 日志、笔记、历史

**Essence（精华）**：
用户以后真正会反复查阅的东西。课程内容很少被回顾，参考文档会被。它是课程内容的精炼，要美观、结合图片、便于快速查阅。它也是制卡的资料来源。
_Avoid_: 笔记、总结、文档

**Material（资料）**：
用户提供或从互联网获取的学习资源，全局共享，以 AI 可读格式存储。需要定期毫不留情地删减——五个精辟的来源胜过三十个平庸的。
_Avoid_: 文件、附件、素材

### 卡片与复习

**Card（卡片）**：
一个值得长期保持的知识单元。正面是问题，背面是参考答案或评价标准，另附 AI 写的制卡原因与补充说明。归属于 Topic，不归属于产出它的对话。它既是复习的核心，也是记录用户已学内容的载体。八股问答、背单词、算法题共用同一种形态，评价方式交给 AI 判断。
_Avoid_: 题目、知识点、flashcard、错题、难题

**Card Proposal（卡片提议）**：
模型提出的、尚未经用户确认的候选卡片。未经确认的提议不参与复习计划。
_Avoid_: 候选卡、草稿卡、自动卡

**Topic（主题）**：
卡片的归属维度，也是复习调度参数的配置粒度。可以细化到任意粒度，例如「后端」「Java」「JVM」，制卡时 AI 优先复用现有主题。
_Avoid_: 分类、标签、类别、领域

**Rating（评级）**：
一次复习的结构化结论，取 `Again / Hard / Good / Easy` 四档，由 AI 判定。它是模型评价与调度计算之间唯一的接口。
_Avoid_: 得分、评分、grade

**Review（复习）**：
针对某张卡片的一次作答与评价。只有评级入库，评语是给用户看的一次性反馈。
_Avoid_: 测验、考试、答题

**Due Queue（到期队列）**：
某一天应当复习的卡片集合。由调度状态确定性地算出，不由模型挑选。
_Avoid_: 今日任务、复习列表

### 用户模型

**Preference（偏好）**：
用户对讲解方式的稳定要求，例如表达风格、类比禁忌、代码示例习惯。以用户手写为主，写在全局或工作区级的用户文件里。
_Avoid_: 设置、配置、画像
