# Pi Teacher

一个 Web 学习助手。它通过多种学习风格帮助用户理解知识、练习知识，并让值得长期保持的知识真正留下来。

本文只定义领域语言，不记录任何实现方案、技术选型或字段设计。

## Language

### 核心概念

它们的关系见 [ADR-0010](docs/adr/0010-studyspace-card-session-boundaries.md)、[ADR-0016](docs/adr/0016-space-session-schema.md)。

**Space（空间）**：
三个固定值的分类枚举——学习、复习、助教。不是可创建的实体，而是 Session 的类型标记。
_Avoid_: Workspace、学习空间、StudySpace（已废弃）

**Session Group（会话组）**：
共享同一个工作目录的一组学习会话。它是前端视图，不是数据库实体——由 `work_path` 相同的会话聚合而成。用户新建学习会话时可选择加入现有组（复用资料与上下文）或新开一组。这是原「学习空间」概念的替代物。
_Avoid_: 学习空间、主题目录、项目

**Card（卡片）**：
一个值得长期保持的知识单元。正面是问题，背面是参考答案或评价标准。八股问答与算法题共用同一种形态。卡片归属于 Topic，不归属于产出它的会话。它既是复习的核心，也是记录用户已学内容的载体。
_Avoid_: 题目、知识点、flashcard

**Session（会话）**：
一整场多轮对话，是一次完整的学习、复习或助教活动。它归属于某个 Space，持有工作目录、Pi 会话文件路径、制卡开关、主题过滤、教学风格。助教会话全局唯一，id 固定为 0。
_Avoid_: 对话轮次、conversation、chat

**Teach Style（教学风格）**：
用户可自定义的一段提示词，决定模型在会话中的人格与教法——循序渐进的老师、只答疑的助教、苏格拉底式追问等。挂在会话上，同一会话组内的不同会话可用不同风格。
_Avoid_: 人格、角色、persona、mode

### 分类与调度

**Topic（主题）**：
复习调度的领域分类，也是卡片归属的维度。一个 Topic 持有名称、描述（给 AI 做归类判据用）、以及该主题的 FSRS 参数（`request_retention`、`maximum_interval`）。主题可以细化到任意粒度，例如「后端」「Java」「JVM」，制卡时 AI 读取现有 Topic 列表并优先复用。
_Avoid_: 分类、标签、类别、领域

**Answer Mode（回答模式）**：
卡片的评价方式，取三档：`open_ended`（开放式，AI 解读理解深度）、`exact`（固定答案，判断匹配）、`code`（算法题，评价思路与实现）。它决定了复习时 AI 如何判断用户的回答。
_Avoid_: 卡片类型、题型

**Rating（评级）**：
一次复习的结构化结论，取 `Again / Hard / Good / Easy` 四档。它是模型评价与调度计算之间唯一的接口，不是百分制分数的线性映射。
_Avoid_: 得分、评分、grade

**Due Queue（到期队列）**：
某一天应当复习的卡片集合。由调度状态确定性地算出，不由模型挑选。
_Avoid_: 今日任务、复习列表

**Review（复习）**：
针对某张卡片的一次作答与评价。开放式回答由模型评价，评价结果必须落为结构化记录。
_Avoid_: 测验、考试、答题

### 制卡

**Sticking Point（难题）**：
用户在学习中确实卡住过的具体问题——答错了、追问几轮才明白、或自己说总记不住。它是卡片的主要来源。
_Avoid_: 错题、问题、issue

**Card Proposal（卡片提议）**：
模型提出的、尚未经用户确认的候选卡片。它带一条 `add_reason` 说明为何值得记（例如「这个问题你几次都回答得不够准确」），以及指向来源消息的锚点。未经确认的提议不参与复习计划。
_Avoid_: 候选卡、草稿卡、自动卡

### 对话机制

**Turn（轮次）**：
会话中一次「用户发言 + 助手回应」的来回。周期性提醒以轮次计数。
_Avoid_: 消息、message、round

**Briefing（上下文注入）**：
向模型注入的当前状态。通过 Pi 的 `AGENTS.md` 机制实现：`~/.pi/AGENTS.md` 存全局用户偏好，当前工作目录的 `.pi/AGENTS.md` 存空间特定内容（学习目标、资料索引、难题清单、教学风格）。这些内容不受 Pi 自动压缩影响，每轮都会加载。
_Avoid_: 系统提示词、上下文窗口、历史摘要

**Reminder（提醒）**：
按轮次注入给模型的隐式提示，用于让模型考虑是否需要维护卡片或偏好。提醒的接收者是模型，不是用户；用户界面不因提醒而弹窗。
_Avoid_: 通知、prompt、催促

### 会话类型

**学习 / 复习 / 助教**：
Session 的三种类型。学习会话归属某个会话组、可制卡；复习会话在固定的复习目录运行、按主题过滤到期卡、可制卡但须克制；助教会话全局唯一、独立上下文、不制卡、压缩更激进，可按需注入当前学习会话的上下文。
_Avoid_: 学习模式、复习模式、学习空间、复习空间

### 学习产出

**Note（笔记）**：
用户在学习过程中记下的理解。

### 用户模型

**Preference（偏好）**：
用户对讲解方式的稳定要求，例如表达风格、类比禁忌、代码示例习惯。以用户手写为主。
_Avoid_: 设置、配置、画像

**Preference Proposal（偏好提议）**：
模型观察到的、尚未经用户确认的候选偏好。未经确认不生效。
_Avoid_: 自动偏好、推断设置
