# 镜像构建上下文：只送进 Dockerfile 真正 COPY 的内容（server/、web/、skills/、docker-entrypoint.sh）。
.git
.gitignore
.scratch
.claude
**/node_modules
**/dist
**/dev-data
**/.env

# 文档不进镜像：出厂提示词已收进 server/src/prompts/defaults.ts。
docs

# 默认挂载目录（compose.yaml 首次启动在仓库旁生成），绝不能打进镜像。
pi-teacher
pi-agent

# 根目录草稿与说明（*.md 只匹配根目录，skills/*/SKILL.md 不受影响）。
*.md
Dockerfile
compose.yaml
